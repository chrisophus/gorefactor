# Phase 2: fine-tuning pipeline dependencies
#
# Install: pip install -r sweagent/finetune/requirements.txt
#
# GPU memory guide:
#   Qwen2.5-Coder-7B  LoRA (r=16): ~20 GB VRAM  (1× A100 40GB, or 2× 3090)
#   Qwen2.5-Coder-7B  LoRA (r=16): ~12 GB VRAM  (1× 3080 12GB with bf16+grad-ckpt)

# Data collection & conversion
pyyaml>=6.0
tqdm>=4.66

# Training
torch>=2.3.0
transformers>=4.45.0
datasets>=2.20.0
peft>=0.12.0          # LoRA
trl>=0.11.0           # SFTTrainer
accelerate>=0.34.0    # multi-GPU / gradient accumulation
bitsandbytes>=0.43.0  # 4-bit quantisation (optional, reduces VRAM)

# Serving (after training)
# vllm>=0.6.0         # uncomment when ready to serve
